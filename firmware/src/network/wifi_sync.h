// WiFi sync (PROTOCOL.md: scan-first sync with backoff, GET /sync/roster,
// POST /sync/submissions). Not implemented yet — later phase, once the
// judging UI exists and there's something worth syncing. Placeholder so
// the directory structure matches the project layout now.
#pragma once
