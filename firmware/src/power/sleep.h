// Sleep / power management (light sleep between judging actions to extend
// the 4400mAh LiPo's runtime across a full show day). Not implemented yet
// — later phase, and depends on battery.h being unblocked first.
// Placeholder so the directory structure matches the project layout now.
#pragma once
