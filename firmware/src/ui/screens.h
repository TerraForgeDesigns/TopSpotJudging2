// Judging UI screens (numeric keypad for reg-number entry, scoring, photo
// capture flow, Daylight Mode toggle — see DESIGN.md handheld section).
// Not implemented yet — later phase, once display/touch/SD/camera
// bring-up (this phase) is verified on real hardware. Placeholder so the
// directory structure matches the project layout now.
#pragma once
